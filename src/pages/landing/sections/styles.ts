/**
 * The three class strings every section of the public page shares.
 *
 * Kept together so the rhythm of the page — the vertical air, the eyebrow, the
 * display heading — is one decision instead of eight copies drifting apart.
 */
export const SECTION = "py-16 md:py-24";

export const EYEBROW =
  "mb-3 font-sans text-2xs font-semibold tracking-[0.12em] text-violeta uppercase";

/** Quiet section labels below the hero; its original typography stays intact. */
export const SECTION_LABEL = "mt-0 mb-5 text-sm font-medium text-tinta-suave";

/** The display face, on the landing's large headings only. */
export const HEADING =
  "font-display text-[clamp(1.75rem,3vw,2.4rem)] leading-[1.18] font-semibold tracking-[-0.025em] text-balance";
