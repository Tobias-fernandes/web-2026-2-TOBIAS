/** Prefix for every demo key kept in localStorage. */
export const STORAGE_PREFIX = 'altotech:demo:'

/** Artificial latency so the UI exercises its loading states. */
export const MOCK_LATENCY_MS = 220
